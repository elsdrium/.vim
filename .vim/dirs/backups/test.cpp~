#include <iostream>
#include <vector>
using namespace std;

int main()
{
	void* p = nullptr;
	vector<int> vec;

	vec.push_back( 5 );
	vec.push_back( 5 );
	vec.push_back( 8 );
	for ( auto &i : vec ) {
		cout << i << endl;
	}

	cout << "hello world" << endl;

	return 0;
}
